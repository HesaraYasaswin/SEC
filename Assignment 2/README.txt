to run,

./gradlew run --args="testinput-0.utf8.map"
./gradlew run --args="testinput-1.utf8.map"
./gradlew run --args="testinput-2.utf8.map"


includes the script logic as well


if the plugin mode is active, u will have to press q again to quit from the plugin mode to the core mode.


for the java cc warnings,
it works eventhough it shows warnings.
after all the warnings are displayed, the game will be started.
